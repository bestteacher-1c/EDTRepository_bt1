// Описание
// 
// Параметры:
// 	Форма - УправляемаяФорма - Форма в которой нужно управлять размером
&НаСервере
Процедура ВосстановитьЭталонныйРазмерОкнаДляФормы(Форма) Экспорт

	ХранилищеСистемныхНастроек.Удалить(Форма.ИмяФормы, , ИмяПользователя());
	Форма.КлючСохраненияПоложенияОкна = Строка(Новый УникальныйИдентификатор);

КонецПроцедуры	
		
